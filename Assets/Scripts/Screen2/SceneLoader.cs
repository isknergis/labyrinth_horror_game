using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneLoader : MonoBehaviour
{
   /* public Transform spawnPoint; // SpawnPoint GameObject'inin Transform bileþenini buraya atayýn

    void Start()
    {
        GameObject player = GameObject.FindWithTag("Player");
        if (player != null)
        {
            CharacterController controller = player.GetComponent<CharacterController>();

            if (PlayerPositionManager.hasSavedPosition)
            {
                controller.enabled = false;
                player.transform.position = PlayerPositionManager.playerPosition;
                player.transform.rotation = PlayerPositionManager.playerRotation;
                controller.enabled = true;
            }
            else if (spawnPoint != null)
            {
                controller.enabled = false;
                player.transform.position = spawnPoint.position;
                player.transform.rotation = spawnPoint.rotation;
                controller.enabled = true;
            }
        }
    }*/
}

